# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'

target 'quiziine' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for quiziine
  pod 'Alamofire'  
  pod 'SwiftyJSON'

  target 'quiziineTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
